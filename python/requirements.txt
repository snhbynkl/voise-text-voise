# Install the matching Torch/Torchaudio build first; see docs/LOCAL_VOICE_CLONE.md.
coqui-tts==0.27.5
# Coqui 0.27.5 has loose upper bounds; these versions are verified with XTTS v2.
transformers==4.57.6
huggingface-hub==0.36.2
tokenizers==0.22.2
